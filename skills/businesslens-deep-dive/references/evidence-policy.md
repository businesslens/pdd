# Evidence policy

- Follow current entity `codeRefs` into implementation and tests.
- Prefer direct handlers, services, guards, persistence, configuration, and
  test cases over prose claims.
- Prefer `path#symbol`; use line ranges only when read from this checkout.
- Treat fixtures and generated code as supporting, not primary, evidence.
- Record ambiguity as a limitation instead of inventing behavior.
